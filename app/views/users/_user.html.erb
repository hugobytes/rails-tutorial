<li>
    <%= gravatar_for user %>
    <%= link_to user.name, user %>
    <% if current_user.admin? && !current_user?(user) %>
        <%= button_to "delete", user, method: :delete, class: "btn btn-danger delete-user-button" %>
    <% end %>
</li>